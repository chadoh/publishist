#class Notifications_Test < ActionMailer::TestCase
  #def setup
    #@composition = Factory.build(:composition)
  #end

  #context "new composition" do
    #should "email the editor the composition" do
      #email = Notifications.new_composition(@composition).deliver

      #assert !ActionMailer::Base.deliveries.empty?
    #end
  #end
#end
