li = $("li#packet_<%= @packet.id %>");
li.addClass('submission').removeClass('packet');
li.attr('id', 'submission_<%= @packet.submission.id %>');
if(<%= Packet.all.collect(&:submission).include? @packet.submission %>) {
  li.remove(); }
else {
  $('ul.submissions').append(li);
}
