li = $("li#packlet_<%= @packlet.id %>");
li.addClass('submission').removeClass('packlet');
li.attr('id', 'submission_<%= @packlet.submission.id %>');
if(<%= @packlet.submission.packlets.present? %>) {
  li.remove(); }
else {
  $('ul.submissions').append(li);
}
