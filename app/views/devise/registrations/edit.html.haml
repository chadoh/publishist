- title "Edit Account"
- content_for :sidebar do
  = render :partial => 'people/your_name_looks_like'
= image_tag @person.gravatar_url, :alt => "A picture of you"
:markdown
  You can change this photo on [Gravatar](http://gravatar.com).
= render 'devise/registrations/form'
-#TODO: allow people to destroy their account without destroying everything they've submitted
-#%h3 Cancel my account
-#%p
  Unhappy? #{link_to "Cancel my account", registration_path(resource_name), :confirm => "Are you sure?", :method => :delete}.
