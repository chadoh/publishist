$("span.score[data-packet=<%= resource.packet.id %>][data-attendance=<%= resource.attendance.id %>]")
  .replaceWith("<%= escape_javascript(
    render :partial => 'form', :locals => { :score => Score.with(resource.attendance, resource.packet) }
  )%>");
