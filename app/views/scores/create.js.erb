$("span.score[data-packlet=<%= resource.packlet.id %>][data-attendee=<%= resource.attendee.id %>]")
  .replaceWith("<%= escape_javascript(
    render :partial => 'form', :locals => { :score => resource }
  )%>");
