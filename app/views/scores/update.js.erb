$("span.score[data-packlet=<%= @score.packlet.id %>][data-attendee=<%= @score.attendee.id %>]")
  .replaceWith("<%= escape_javascript(
    render :partial => 'form', :locals => { :score => @score }
  )%>");
