- title @person.full_name
- if @person == @user || the_editor?
  - content_for :aside do
    = render :partial => 'about', :locals => { :person => @person }
  - if @person == @user
    %p What you've submitted
    - if @compositions.empty?
      :markdown
        Nothing, yet. [Get to it!](#{new_composition_path})
  - else
    %p What #{@person.first_name} has submitted
    - if @compositions.empty?
      %p Nothing yet.
  - @compositions.each do |composition|
    = render :partial => 'compositions/composition', :locals => { :composition => composition }
- else
  = render :partial => 'about', :locals => { :person => @person }
